package mobile

// gomobile exported functions
// TODO(mobile-export-init): Expose Init(configJSON string) to configure logging & endpoints from mobile apps.
// TODO(mobile-export-enroll): Expose EnrollDevice() bridging to enrollment flow.
// TODO(mobile-export-auth): Expose StartAuthChallenge() / VerifyAuthResponse() for challenge-based login.
// TODO(mobile-export-qrcode): Expose GenerateDeviceAddQR() & ParseDeviceAddQR().
// TODO(mobile-export-recovery): Expose GenerateRecoveryCodes() (future) with secure storage hints.
// TODO(mobile-export-session): Expose GetActiveSessions() for display in mobile UI.
// TODO(mobile-export-threading): Ensure exported functions are safe for concurrent calls (document requirements).
